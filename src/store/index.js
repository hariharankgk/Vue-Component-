import Vue from 'vue'
import Vuex from 'vuex'
import VueCarousel from 'vue-ctk-carousel';

Vue.use(Vuex)
Vue.use(VueCarousel);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
